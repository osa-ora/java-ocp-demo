# Java OCP Demo Helm Chart

## Installation

Add this Helm chart to your repository:

```sh
helm repo add my-charts ttps://github.com/osa-ora/java-ocp-demo/helm-chart/
helm repo update
```
to install it:

```
helm install java-ocp-demo my-charts/java-ocp-demo -n your-namespace
```

